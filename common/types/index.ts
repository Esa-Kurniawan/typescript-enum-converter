export enum Language {
    TYPESCRIPT = "typescript",
    JSON = "json",
}

export enum DataType {
    ARRAY_OF_OBJECTS = "array of objects",
    ARRAY_OF_STRINGS = "array of strings",
    OBJECT = "object",
}

export enum KeyOrValue {
    KEY = "key",
    VALUE = "value",
}
